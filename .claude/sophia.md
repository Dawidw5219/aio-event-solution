# Sophia — kontekst projektu `aio-event-solution`

## Proof / weryfikacja wizualna

**Ten projekt NIE wymaga wizualnych screenshotów (reference + result) dla tasków.**

- Każdy task przechodzi z `in-progress` bezpośrednio do `human-review`.
- Proof = diff snippet, ścieżka pliku + numer linii, output komendy, opis zmiany.
- Human sam sprawdza wynik w przeglądarce.
- `agent-browser` MCP i komenda `claude4 screenshot` są tu pominięte — opt-out na poziomie projektu.

## Logowanie / URL

Nie ustawione — human weryfikuje na swoim środowisku.
